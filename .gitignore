# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Virtual environments
.venv/
venv/
env/

# Data directories — not committed (too large, often license-restricted).
# Fetch raw data via `python -m popfc.data.download` (for sources with
# automated download specs) or by manual copy from upstream URLs.
data_raw/
data_interim/
data_final/

# Git worktree for the docs/main branch (docs editing from any feature
# branch; docs changes merge to main via PR/merge like code does).
.worktree-docs/

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp

# Build artifacts
build/
dist/
